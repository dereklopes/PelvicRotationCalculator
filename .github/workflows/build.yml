name: Build and Package

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:
    branches: [main, master]

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest, macos-13]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build and test with Maven
        run: mvn -B package --no-transfer-progress

      - name: Package with jpackage (Linux and macOS)
        if: runner.os != 'Windows'
        run: |
          jpackage \
            --type app-image \
            --name PelvicRotationCalculator \
            --app-version 1.0.0 \
            --module-path "target/pelvic-rotation-calculator-1.0.0.jar:target/lib" \
            --module Calculator/Calculator.CalculatorApplication \
            --dest target/dist

      - name: Package with jpackage (Windows)
        if: runner.os == 'Windows'
        run: |
          jpackage `
            --type app-image `
            --name PelvicRotationCalculator `
            --app-version 1.0.0 `
            --module-path "target/pelvic-rotation-calculator-1.0.0.jar;target/lib" `
            --module Calculator/Calculator.CalculatorApplication `
            --dest target/dist

      - name: Archive artifact (Linux and macOS)
        if: runner.os != 'Windows'
        run: zip -r target/PelvicRotationCalculator-${{ matrix.os }}.zip target/dist/

      - name: Archive artifact (Windows)
        if: runner.os == 'Windows'
        run: Compress-Archive -Path target/dist/* -DestinationPath target/PelvicRotationCalculator-${{ matrix.os }}.zip

      - name: Upload packaged application
        uses: actions/upload-artifact@v4
        with:
          name: PelvicRotationCalculator-${{ matrix.os }}
          path: target/PelvicRotationCalculator-${{ matrix.os }}.zip
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/
          merge-multiple: false

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*.zip
          generate_release_notes: true
